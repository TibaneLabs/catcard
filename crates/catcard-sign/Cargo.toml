[package]
name = "catcard-sign"
description = "Deterministic ECDSA and Schnorr signing for Bitcoin"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[features]
default = ["std"]
std = []

[dependencies]
purecrypto = { workspace = true, features = ["hash", "ec", "hazmat-secp256k1", "bip340"] }
