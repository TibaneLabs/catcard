[package]
name = "catcard-sign"
description = "Deterministic ECDSA and Schnorr signing for Bitcoin"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[features]
default = ["std"]
std = []

[dependencies]
k256 = { version = "0.14.0", default-features = false, features = ["arithmetic", "ecdsa", "schnorr"] }
sha2.workspace = true
